import React from 'react';
/*
const NumericSlider = ({props}) => {
  const {value, name} = props;

  const min = props.min || 0;
  const max = props.max || 100;

  const onInputChange = (event) => { props.onValueChange && props.onValueChange(event.target.value); }

  return (
    <div>
      <span>{name}</span>
      <input type="range" min={min} max={max} value={value} className='NumericSlider' onChange='onInputChange'/>
      <input value={value} onChange='onInputChange'/>
    </div>
  );
}*/

const NumericSlider = (props) => {
  return (
    <div></div>
  );
}

export default NumericSlider;
